# Example for API-only VPS (frontend on Vercel).
# Copy to .env next to docker-compose.yml.

CORS_ORIGINS=https://pocasi.smht.eu

# Optional provider keys (same as root .env.example).
# OPENWEATHERMAP_KEY=
# OPENTRIPMAP_API_KEY=
